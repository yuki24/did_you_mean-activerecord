sqlite3:
  adapter: sqlite3
  database: ":memory:"

mysql2:
  adapter: mysql2
  database: activerecord_correctable
  username:
  encoding: utf8

postgres:
  adapter: postgresql
  database: activerecord_correctable
  username: <%= "postgres" if ENV["TRAVIS"] %>
  min_messages: warning
